apiVersion: v1
kind: Service
metadata:
    name: openclaw
    namespace: default
spec:
    selector:
        app: openclaw-ma-npm
    ports:
        - port: 18789
          targetPort: 18789
          protocol: TCP
    type: NodePort
