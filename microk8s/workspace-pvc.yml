apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: pv-workspaces
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 10Gi
    volumeName: pv-workspaces
